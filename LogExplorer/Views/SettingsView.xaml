<views:MvxWpfView x:Class="LogExplorer.Views.SettingsView"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:local="clr-namespace:LogExplorer.Views"
                  xmlns:views="clr-namespace:MvvmCross.Wpf.Views;assembly=MvvmCross.Wpf"
                  mc:Ignorable="d"
                  d:DesignHeight="300" d:DesignWidth="300">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="30" />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>


		<Grid Grid.ColumnSpan="2">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>
			<TextBlock Margin="4" Grid.Row="0" Grid.ColumnSpan="2" Text="Root logs path:" />
			<TextBox Margin="4" Grid.Row="1" Grid.Column="0"
					         Text="{Binding RootLogsPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Stretch" />
            <Button Margin="4" Grid.Row="1" Grid.Column="1" Command="{Binding CmdPickRootDir}" HorizontalAlignment="Right" Content=".."/>
            
            <TextBlock Margin="4" Grid.Row="2" Grid.ColumnSpan="2" Grid.Column="0" Text="Export logs path:" />
            <TextBox Margin="4" Grid.Row="3" Grid.Column="0"
					         Text="{Binding ExportPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Stretch" />
            <Button Margin="4" Grid.Row="3" Grid.Column="1" Command="{Binding CmdPickExportDir}" HorizontalAlignment="Right" Content=".."/>

        </Grid>


		<Button Margin="5,5" Grid.Row="1" Grid.Column="0" Content="Cancel" Command="{Binding CmdCancel}" />
		<Button Margin="5,5" Grid.Row="1" Grid.Column="1" Content="Save" Command="{Binding CmdSave}" />
	</Grid>
</views:MvxWpfView>